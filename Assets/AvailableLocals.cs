using UnityEngine;

public class AvailableLocals : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public GameObject[] Close= new GameObject[3];

}
